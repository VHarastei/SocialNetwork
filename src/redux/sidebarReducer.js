let initialState = {
  friends: [
    { id: 1, name: 'Rosdick' },
    { id: 2, name: 'Kerets' },
    { id: 3, name: 'booba' },
  ],
};

let sidebarReducer = (state = initialState, action) => {
  return state;
};

export default sidebarReducer;
