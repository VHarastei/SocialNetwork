let initialState = {
  friends: [
    { id: 1, name: 'biba' },
    { id: 2, name: 'boba' },
    { id: 3, name: 'booba' },
  ],
};

let sidebarReducer = (state = initialState, action) => {
  return state
}

export default sidebarReducer;
